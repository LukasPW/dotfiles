add_newline = false

format = "$custom$username$hostname$directory$git_branch $character"

[username]
show_always = false
style_user = "#a2c9fe"
style_root = "bold red"
format = "[$user]($style)"

[hostname]
ssh_only = true
style = "#a2c9fe"
format = "[@$hostname ]($style)"

[directory]
style = "#a2c9fe"
format = "[$path]($style)"
truncation_length = 0
truncate_to_repo = false

[git_branch]
symbol = ""
style = "#bbc7db"
format = " ([($branch)]($style))"

[character]
success_symbol = "[➜](bold #bbc7db)"
error_symbol = "[➜](bold red)"
vicmd_symbol = "[❮](bold yellow)"

[custom.logo]
command = "echo "
when = "true"
style ="#a2c9fe"
format = "[$output]($style) "
