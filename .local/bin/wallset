#!/usr/bin/env bash

img="${1:-$(fd . /home/aswdxtbyyn/Pictures/wallpapers/ | fzf)}"

[ -z "$img" ] && exit 1

awww img "$img"
matugen image "$img" --source-color-index 0
hyprctl reload
pkill -SIGUSR2 -f waybar
